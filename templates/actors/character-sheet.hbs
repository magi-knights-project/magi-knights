<form class="{{cssClass}} flexcol" autocomplete="off">

    {{!-- Sheet Header --}}
    <header class="sheet-header flexrow">
        <img class="profile" src="{{actor.img}}" title="{{actor.name}}" data-edit="img"/>

        <section class="header-details flexrow">
            {{> "mka.actor-warnings"}}

            <h1 class="charname">
                <input name="name" type="text" value="{{actor.name}}" placeholder="{{ localize 'MKA.Name' }}"/>
            </h1>

            <aside class="header-exp flexcol">
                <div class="charlevel" title="{{multiclassLabels}}">
                    <label>{{ localize "MKA.Level" }} {{system.details.level}}</label>
                    <span class="levels">{{classLabels}}</span>
                </div>

                {{#unless disableExperience}}
                <div class="experience flexrow">
                    {{numberInput system.details.xp.value name="system.details.xp.value" placeholder="0"}}
                    <span class="sep">/</span>
                    <span class="max">{{system.details.xp.max}}</span>
                </div>
                <div class="xpbar">
                    <span class="bar" style="width: {{system.details.xp.pct}}%"></span>
                </div>
                {{/unless}}
            </aside>

            {{!-- Character Summary --}}
            <ul class="summary flexrow">
                <li>
                    <input type="text" name="system.details.stype" value="{{system.details.stype}}" placeholder="{{ localize 'MKA.SType' }}"/>
                </li>
                <li>
                    {{#if labels.background}}
                        <span title="{{localize 'MKA.Background'}}">{{labels.background}}</span>
                    {{else}}
                        <input type="text" name="system.details.background" value="{{system.details.background}}" placeholder="{{ localize 'MKA.Background' }}"/>
                    {{/if}}
                </li>
                <li>
                    <input type="text" name="system.details.alignment" value="{{system.details.alignment}}" placeholder="{{ localize 'MKA.Alignment' }}"/>
                </li>
                <li class="proficiency">
                    <span>{{ localize "MKA.Proficiency" }} {{labels.proficiency}}</span>
                </li>
            </ul>

            {{!-- Header Attributes --}}
            <ul class="attributes flexrow">
                <li class="attribute health">
                    <h4 class="attribute-name box-title">{{ localize "MKA.HitPoints" }}</h4>
                    <div class="attribute-value multiple">
                        <input name="system.attributes.hp.value" type="number" step="any"
                            value="{{system.attributes.hp.value}}" placeholder="10"
                            title="{{localize 'MKA.HitPointsCurrent'}}">
                        <span class="sep"> / </span>
                        <input name="system.attributes.hp.max" type="number" step="any"
                            value="{{system.attributes.hp.max}}" placeholder="10"
                            title="{{localize 'MKA.HitPointsMax'}}">
                    </div>
                    <footer class="attribute-footer">
                        <input name="system.attributes.hp.temp" type="number" step="any" class="temphp"
                            placeholder="+{{ localize 'MKA.Temp' }}"
                            value="{{system.attributes.hp.temp}}"
                            title="{{localize 'MKA.HitPointsTemp'}}">
                        <input name="system.attributes.hp.tempmax" type="number" step="any" class="temphp"
                            placeholder="+{{ localize 'MKA.Max' }}"
                            value="{{system.attributes.hp.tempmax}}"
                            title="{{localize 'MKA.HitPointsTempMax'}}">
                    </footer>
                </li>

                <li class="attribute hit-dice">
                    <h4 class="attribute-name box-title">
                        {{ localize "MKA.HitDice" }}
                        <a class="config-button" data-action="hit-dice" title="{{localize 'MKA.HitDiceConfig'}}"><i class="fas fa-cog"></i></a>
                    </h4>
                    <div class="attribute-value multiple">
                        <label class="hit-dice">
                            <span title="{{localize 'MKA.HitDiceRemaining'}}">{{system.attributes.hd}}</span>
                            <span class="sep"> / </span>
                            <span title="{{localize 'MKA.HitDiceMax'}}">{{system.details.level}}</span>
                        </label>
                    </div>
                    <footer class="attribute-footer">
                        <a class="rest short-rest" title="{{localize 'MKA.ShortRest'}}">{{ localize "MKA.RestS" }}</a>
                        <a class="rest long-rest" title="{{localize 'MKA.LongRest'}}">{{ localize "MKA.RestL" }}</a>
                    </footer>
                </li>

                <li class="attribute armor">
                    <h4 class="attribute-name box-title">
                        {{ localize "MKA.ArmorClass" }}
                        <a class="config-button" data-action="armor" title="{{localize 'MKA.ArmorConfig'}}"><i class="fas fa-cog"></i></a>
                    </h4>
                    <div class="attribute-value attributable" data-property="attributes.ac">
                        <span>{{system.attributes.ac.value}}</span>
                    </div>
                    <footer class="attribute-footer">
                        <span class="spell-dc">{{localize "MKA.SpellDC"}} {{system.attributes.spelldc}}</span>
                    </footer>
                </li>

                <li class="attribute movement">
                    <h4 class="attribute-name box-title">
                        {{ localize "MKA.Movement" }}
                        <a class="config-button" data-action="movement" title="{{localize 'MKA.MovementConfig'}}"><i class="fas fa-cog"></i></a>
                    </h4>
                    <div class="attribute-value">
                        <span>{{movement.primary}}</span>
                    </div>
                    <footer class="attribute-footer">
                        <span>{{movement.special}}</span>
                    </footer>
                </li>

                <li class="attribute initiative">
                    <h4 class="attribute-name box-title rollable" data-action="rollInitiative">{{ localize "MKA.Initiative" }}</h4>
                    <div class="attribute-value">
                        <span>{{numberFormat system.attributes.init.total decimals=0 sign=true}}</span>
                    </div>
                    <footer class="attribute-footer">
                        <span>{{ localize "MKA.Modifier" }}</span>
                        <input name="system.attributes.init.value" type="text" data-dtype="Number" placeholder="0"
                               value="{{numberFormat system.attributes.init.value decimals=0 sign=true}}"/>
                    </footer>
                </li>
            </ul>

            
        </section>

    </header>

        {{!-- Secondary Attributes --}}
            <ul class="attributes flexrow">
                <li class="attribute unity">
                    <h4 class="attribute-name box-title">{{ localize "MKA.Unity" }}</h4>
                    <div class="attribute-value multiple">
                        <input name="system.attributes.unity.value" type="number" step="any"
                            value="{{system.attributes.unity.value}}" placeholder="0"
                            title="{{localize 'MKA.UnityPointsCurrent'}}">
                        <span class="sep"> / </span>
                        <input name="system.attributes.unity.max" type="number" step="any"
                            value="{{system.attributes.unity.max}}" placeholder="4"
                            title="{{localize 'MKA.UnityPointsMax'}}">
                    </div>
                </li>

                <li class="attribute mp">
                    <h4 class="attribute-name box-title">{{ localize "MKA.MP" }}</h4>
                    <div class="attribute-value multiple">
                        <input name="system.attributes.mp.value" type="number" step="any"
                            value="{{system.attributes.mp.value}}" placeholder="0"
                            title="{{localize 'MKA.MagicPointsCurrent'}}">
                        <span class="sep"> / </span>
                        <input name="system.attributes.mp.max" type="number" step="any"
                            value="{{system.attributes.mp.max}}" placeholder="10"
                            title="{{localize 'MKA.MagicPointsMax'}}">
                    </div>
                </li>

                <li class="attribute exhaustion">
                    <h4 class="attribute-name box-title">{{ localize "MKA.Exhaustion" }}</h4>
                    <div class="attribute-value multiple">
                        <input name="system.attributes.exhaustion.value" type="number" step="any"
                            value="{{system.attributes.exhaustion.value}}" placeholder="0"
                            title="{{localize 'MKA.ExhaustionPointsCurrent'}}">
                        <span class="sep"> / </span>
                        <input name="system.attributes.exhaustion.max" type="number" step="any"
                            value="{{system.attributes.exhaustion.max}}" placeholder="6"
                            title="{{localize 'MKA.ExhaustionPointsMax'}}">
                    </div>
                </li>

                <li class="attribute stress">
                    <h4 class="attribute-name box-title">{{ localize "MKA.Stress" }}</h4>
                    <div class="attribute-value multiple">
                        <input name="system.attributes.stress.value" type="number" step="any"
                            value="{{system.attributes.stress.value}}" placeholder="0"
                            title="{{localize 'MKA.StressPointsCurrent'}}">
                        <span class="sep"> / </span>
                        <input name="system.attributes.stress.max" type="number" step="any"
                            value="{{system.attributes.stress.max}}" placeholder="6"
                            title="{{localize 'MKA.StressPointsMax'}}">
                    </div>
                </li>

                <li class="attribute trauma">
                    <h4 class="attribute-name box-title">{{ localize "MKA.Trauma" }}</h4>
                    <div class="attribute-value multiple">
                        <input name="system.attributes.trauma.value" type="number" step="any"
                            value="{{system.attributes.trauma.value}}" placeholder="0"
                            title="{{localize 'MKA.TraumaPointsCurrent'}}">
                        <span class="sep"> / </span>
                        <input name="system.attributes.trauma.max" type="number" step="any"
                            value="{{system.attributes.trauma.max}}" placeholder="8"
                            title="{{localize 'MKA.TraumaPointsMax'}}">
                    </div>
                </li>

                <li class="attribute corruption">
                    <h4 class="attribute-name box-title">{{ localize "MKA.Corruption" }}</h4>
                    <div class="attribute-value multiple">
                        <input name="system.attributes.corruption.value" type="number" step="any"
                            value="{{system.attributes.corruption.value}}" placeholder="0"
                            title="{{localize 'MKA.CorruptionPointsCurrent'}}">
                        <span class="sep"> / </span>
                        <input name="system.attributes.corruption.max" type="number" step="any"
                            value="{{system.attributes.corruption.max}}" placeholder="4"
                            title="{{localize 'MKA.CorruptionPointsMax'}}">
                    </div>
                </li>
            </ul>
    

    {{!-- Character Sheet Navigation --}}
    <nav class="sheet-navigation tabs" data-group="primary">
        <a class="item active" data-tab="attributes">{{ localize "MKA.Attributes" }}</a>
        <a class="item" data-tab="inventory">{{ localize "MKA.Inventory" }}</a>
        <a class="item" data-tab="features">{{ localize "MKA.Features" }}</a>
        <a class="item" data-tab="spellbook">{{ localize "MKA.Spellbook" }}</a>
        <a class="item" data-tab="effects">{{ localize "MKA.Effects" }}</a>
        <a class="item" data-tab="biography">{{ localize "MKA.Biography" }}</a>
    </nav>

    {{!-- Character Sheet Body --}}
    <section class="sheet-body">

        <div class="tab attributes flexrow" data-group="primary" data-tab="attributes">

            {{!-- Ability Scores --}}
            <ul class="ability-scores flexrow">
            {{#each system.abilities as |ability id|}}
                <li class="ability {{#if ability.proficient}}proficient{{/if}}" data-ability="{{id}}">
                    <h4 class="ability-name box-title rollable">{{ability.label}}</h4>
                    {{numberInput ability.value name=(concat "system.abilities." id ".value")
                        class="ability-score" placeholder="10"}}
                    <div class="ability-modifiers flexrow">
                        <span class="ability-mod" title="{{ localize 'MKA.Modifier' }}">{{numberFormat ability.mod decimals=0 sign=true}}</span>
                        <input type="hidden" name="system.abilities.{{id}}.proficient" value="{{ability.baseProf}}" data-dtype="Number"/>
                        <a class="proficiency-toggle ability-proficiency" title="{{ localize 'MKA.Proficiency' }}">{{{ability.icon}}}</a>
                        <span class="ability-save" title="{{ localize 'MKA.SavingThrow' }}">{{numberFormat ability.save decimals=0 sign=true}}</span>
                    </div>
                    <a class="config-button" data-action="ability" title="{{localize 'MKA.AbilityConfigure'}}"><i class="fas fa-cog"></i></a>
                </li>
            {{/each}}
            </ul>

            {{!-- Skills --}}
            <ul class="skills-list">
            {{#each config.skills as |obj s|}}
            {{#with (lookup ../system.skills s) as |skill|}}
                <li class="skill flexrow {{#if skill.value}}proficient{{/if}}" data-skill="{{s}}">
                    <input type="hidden" name="system.skills.{{s}}.value" value="{{skill.baseValue}}" data-dtype="Number"/>
                    <a class="proficiency-toggle skill-proficiency" title="{{skill.hover}}">{{{skill.icon}}}</a>
                    <div class="skill-name-controls">
                      <h4 class="skill-name rollable">{{skill.label}}</h4>
                      <a class="config-button" data-action="skill" title="{{localize 'MKA.SkillConfigure'}}"><i class="fas fa-cog"></i></a>
                    </div>
                    <span class="skill-ability">{{skill.ability}}</span>
                    <span class="skill-mod" title="{{ localize 'MKA.SkillModifierHint' skill=skill.label }}">
                        {{numberFormat skill.total decimals=0 sign=true}}
                    </span>
                    <span class="skill-passive" title="{{ localize 'MKA.SkillPassiveHint' skill=skill.label }}">
                        ({{skill.passive}})
                    </span>
                </li>
            {{/with}}
            {{/each}}
            </ul>

            <section class="center-pane flexcol">

                {{!-- Body Attributes --}}
                <ul class="attributes flexrow">
                    {{#each resources as |res|}}
                    <li class="attribute resource">
                        <h4 class="attribute-name box-title">
                            <input name="system.resources.{{res.name}}.label" type="text" value="{{res.label}}"
                                   placeholder="{{res.placeholder}}" />
                        </h4>
                        <div class="attribute-value">
                            <label class="recharge checkbox flexcol">
                                <span>{{ localize "MKA.AbbreviationSR" }}</span><input name="system.resources.{{res.name}}.sr" type="checkbox" {{checked res.sr}}/>
                            </label>

                            {{numberInput res.value name=(concat "system.resources." res.name ".value")
                                placeholder="0"}}
                            <span class="sep"> / </span>
                            {{numberInput res.max name=(concat "system.resources." res.name ".max") placeholder="0"}}

                            <label class="recharge checkbox flexcol">
                                <span>{{ localize "MKA.AbbreviationLR" }}</span><input name="system.resources.{{res.name}}.lr" type="checkbox" {{checked res.lr}}/>
                            </label>
                        </div>
                    </li>
                    {{/each}}
                </ul>

                {{!-- Counters --}}
                <div class="counters">
                    <div class="counter flexrow death-saves">
                        <h4 class="rollable" data-action="rollDeathSave">{{ localize "MKA.DeathSave" }}</h4>
                        <div class="counter-value">
                            <i class="fas fa-check"></i>
                            {{numberInput system.attributes.death.success name="system.attributes.death.success"
                                placeholder="0"}}
                            <i class="fas fa-times"></i>
                            {{numberInput system.attributes.death.failure name="system.attributes.death.failure"
                                placeholder="0"}}
                        </div>
                    </div>
                    <div class="counter flexrow exhaustion">
                        <h4>{{ localize "MKA.Exhaustion" }}</h4>
                        <div class="counter-value">
                            {{numberInput system.attributes.exhaustion name="system.attributes.exhaustion"
                                placeholder="0"}}
                        </div>
                    </div>
                    <div class="counter flexrow inspiration">
                        <h4>{{ localize "MKA.Inspiration" }}</h4>
                        <div class="counter-value">
                            <input type="checkbox" name="system.attributes.inspiration" data-dtype="Boolean"
                                   {{checked system.attributes.inspiration}}/>
                        </div>
                    </div>
                </div>

                {{!-- Traits --}}
                {{> "mka.actor-traits"}}
            </section>
        </div>

        {{!-- Inventory Tab --}}
        <div class="tab inventory flexcol" data-group="primary" data-tab="inventory">
            {{> "mka.actor-inventory" sections=inventory}}
        </div>

        {{!-- Features Tab --}}
        <div class="tab features flexcol" data-group="primary" data-tab="features">
            {{> "mka.actor-features" sections=features}}
        </div>

        {{!-- Spellbook Tab --}}
        <div class="tab spellbook flexcol" data-group="primary" data-tab="spellbook">
            {{> "mka.actor-spellbook"}}
        </div>

        {{!-- Effects Tab --}}
        <div class="tab effects flexcol" data-group="primary" data-tab="effects">
            {{> "mka.active-effects"}}
        </div>

        {{!-- Biography Tab --}}
        <div class="tab flexrow" data-group="primary" data-tab="biography">
            <div class="characteristics flexcol">
                <label>{{ localize "MKA.Appearance" }}</label>
                <textarea name="system.details.appearance">{{system.details.appearance}}</textarea>
                <label>{{ localize "MKA.PersonalityTraits" }}</label>
                <textarea name="system.details.trait">{{system.details.trait}}</textarea>
                <label>{{ localize "MKA.Ideals" }}</label>
                <textarea name="system.details.ideal">{{system.details.ideal}}</textarea>
                <label>{{ localize "MKA.Bonds" }}</label>
                <textarea name="system.details.bond">{{system.details.bond}}</textarea>
                <label>{{ localize "MKA.Flaws" }}</label>
                <textarea name="system.details.flaw">{{system.details.flaw}}</textarea>
            </div>
            <div class="biography">
                {{editor biographyHTML target="system.details.biography.value" button=true editable=editable
                         engine="prosemirror" collaborate=false}}
            </div>
        </div>
    </section>
</form>
